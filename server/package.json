{
  "name": "server",
  "version": "1.0.0",
  "description": "tree_api",
  "main": "index.js",
  "scripts": {
    "start": "cross-env PGUSER=postgres PGPASSWORD=example PGHOST=localhost PGPORT=5432 PGDATABASE=tree nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "express-promise-router": "^3.0.3",
    "pg": "^7.4.3"
  },
  "devDependencies": {
    "cross-env": "^5.2.0"
  }
}
